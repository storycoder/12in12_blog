<%= form_for([@post, @post.comments.build]) do |f| %>
	<p>
		<%= f.label :name %><br>
		<%= f.text_field :name %>
	</p>
	<p>
		<%= f.label :comment_body %><br>
		<%= f.text_area :comment_body %>
	</p>
	<br>
	<p>
		<%= f.submit %>
	</p>
<% end %>